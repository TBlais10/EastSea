CREATE TABLE `eastsea_record` (
  `id` mediumint(8) unsigned NOT NULL,
  `year` varchar(255),
  `month` varchar(255),
  `day` varchar(255),
  `lat` varchar(255),
  `lon` varchar(255),
  `vgs` varchar(255),
  `ugs` varchar(255),
  `adts` varchar(255),
  PRIMARY KEY (`id`)
) AUTO_INCREMENT=1;