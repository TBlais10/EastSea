CREATE TABLE `eastsea_record` (
  `year` varchar(255),
  `month` varchar(255),
  `day` varchar(255),
  `lat` varchar(255),
  `lon` varchar(255),
  `vgs` varchar(255),
  `ugs` varchar(255),
  `adts` varchar(255),
) AUTO_INCREMENT=1;